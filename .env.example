# Hyperliquid Trade Ledger API - Environment Variables

# Server port (default: 3000)
PORT=3000

# Builder address for builderOnly filtering (optional)
# Set this to filter trades by a specific builder
# Example: TARGET_BUILDER=0x1234567890abcdef1234567890abcdef12345678
TARGET_BUILDER=

# Datasource type (default: hyperliquid)
# Options: hyperliquid, insilico-hl, hyperserve
DATASOURCE_TYPE=hyperliquid

# Hyperliquid API base URL (default: https://api.hyperliquid.xyz)
# Change this for testnet: https://api.hyperliquid-testnet.xyz
HYPERLIQUID_BASE_URL=https://api.hyperliquid.xyz

# Cache settings
CACHE_FILLS_TTL_MS=60000
CACHE_CLEARINGHOUSE_TTL_MS=5000

# Max starting capital for return percentage calculation (capped normalization)
# This ensures fair comparison by capping effective capital
MAX_START_CAPITAL=1000000

# Log level (default: info)
# Options: fatal, error, warn, info, debug, trace
LOG_LEVEL=info
